let express,API,AccountManager;_625‍.w("dotenv/config");_625‍.w("express",[["default",["express"],function(v){express=v}]]);_625‍.w("./api",[["default",["API"],function(v){API=v}]]);_625‍.w("./mongo/account",[["default",["AccountManager"],function(v){AccountManager=v}]]);





const app = express();
const port = process.env.PORT;

app.use(express.json());
app.use(API);

app.listen(port, () => {
    console.log(`App listening on port ${port}`);

    setInterval(() => {
        console.log('Performing hourly token check on database...');
        AccountManager.checkExpiredTokens();
    }, 1000 * 60 * 60);
});